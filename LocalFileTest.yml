---
- name: Test File Transfer from Local Server
  hosts: all
  become: true
  vars:
  
  tasks:
  - name: Copy SSSD Config to /etc/sssd
    copy:
      src: /AnsibleFS/testfile.test
      dest: /tmp/testfile.test
